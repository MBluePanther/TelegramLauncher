<UserControl x:Class="TelegramLauncher.Views.Controls.ThemeBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             MinHeight="44">
  <Grid Margin="0,4,0,0">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
      <TextBlock Text="Тема" FontWeight="SemiBold" Margin="0,0,8,0" VerticalAlignment="Center"/>
      <ComboBox x:Name="AccentBox" Width="140" Margin="0,0,8,0"/>
    </StackPanel>

    <mah:ToggleSwitch x:Name="BaseToggle"
                      Grid.Column="1"
                      Margin="8,0,0,0"
                      VerticalAlignment="Center"
                      OnContent="On"
                      OffContent="Off"/>
  </Grid>
</UserControl>
